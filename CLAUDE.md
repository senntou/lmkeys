このディレクトリでは、python製のツールを作成する。
名前はlmkeys。

ディレクトリを指定し、そのディレクトリのLMDBについて、keyの一覧をguiで確認するだけのツール。
keyは上から順に読み込み、最初は30件だけ取得し表示。
hlで左右にページを切り替える。ページ切り替えで次の30件を取得。
キーは最初に一括で読み込まず、毎回動的に取得するのが良いか？と思っている。

キーの横には値について表示するが、中身が何なのかわからないので、型だけ表示が良いかと思っている。
pip installでインストール可能にしたい。

